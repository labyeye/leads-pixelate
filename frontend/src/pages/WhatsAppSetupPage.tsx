/**
 * WhatsApp Setup page — connection, templates, config.
 * Wraps the existing WhatsappSettingsPage content under the new /whatsapp/setup route.
 */
export { default } from "./WhatsappSettingsPage";
